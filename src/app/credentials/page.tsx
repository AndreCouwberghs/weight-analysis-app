import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Credentials",
};

// `app/credentials/page.tsx` is the UI for the `/credentials` URL
export default function Page() {
  return <h1>Credentials</h1>;
}
