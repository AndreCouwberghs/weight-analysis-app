import styles from "./page.module.css";

// `app/page.tsx` is the UI for the `/` URL
export default function Home() {
  return (
    <main className={styles.test}>
      <div>Weight Analysis App</div>
    </main>
  );
}
